[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "driftfix-demo-target"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["stripe==14.3.0"]

[project.optional-dependencies]
dev = ["pytest"]

[tool.setuptools]
py-modules = ["customer_service"]
